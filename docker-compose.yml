services:
  web:
    build: .
    container_name: proyecto3_app
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./proyecto_3:/app
    ports:
      - "8000:8000"
    env_file:
      - .env